pipeline {
  agent any
  stages {
    stage('Clean') {
      steps { sh 'git clean -ffdx' }
    }
    stage('Build') {
      steps {
        sh 'make -j 8 all'
        // archiveArtifacts artifacts: 'Analysis.pdf', onlyIfSuccessful: true
      }
    }
  }
}
