pipeline {
  agent any
  stages {
    stage('Clean') {
      steps { sh 'git clean -ffdx' }
    }
    stage('Setup Environment') {
      steps {
        sh 'Rscript -e \"source(\'packrat/init.R\')\"'
      }
    }
    stage('Test') {
      steps {
        sh 'Rscript --default-packages=methods -e \"devtools::test(\'./fcrn\')\"'
      }
    }
    stage('Build') {
      steps {
        sh 'echo Build'
      }
    }
  }
}
