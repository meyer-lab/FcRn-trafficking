---
title: "R Notebook"
output: html_notebook
---

```{r, include=TRUE}
library(rstan)

rstan_options(auto_write = T)
options(mc.cores = parallel::detectCores())

source("data/data.R")
```

AA

```{r}
fitopt <- optimizing(stan_model("model/diff.stan"), data = ddata, verbose = T, hessian = T)
```

XX

```{r}
load("samples.rds")
pairs(fit)
```
